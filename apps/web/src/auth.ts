/**
 * Next.js wrapper around the shared Auth.js config.
 * Importing from "@quizelo/auth" gives us auth(), handlers, signIn, signOut.
 */
export { auth, handlers, signIn, signOut } from "@quizelo/auth";
